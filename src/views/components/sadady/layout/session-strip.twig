<section class="session-strip" data-sadady-session-strip>
  <div class="session-strip-copy">
    <span class="session-eyebrow">{{ trans('sadady.session.eyebrow') }}</span>
    <strong data-sadady-session-title>{{ trans('sadady.session.disconnected') }}</strong>
    <p data-sadady-session-subtitle>{{ trans('sadady.session.subtitle') }}</p>
  </div>
  <div class="session-strip-pills">
    <span class="session-pill" data-sadady-session-name>{{ trans('sadady.session.name') }}</span>
    <span class="session-pill" data-sadady-session-phone>{{ trans('sadady.session.mobile') }}</span>
    <span class="session-pill" data-sadady-session-id>{{ trans('sadady.session.id') }}</span>
  </div>
</section>
