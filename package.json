{
  "name": "sadady-salla-theme",
  "version": "0.3.3",
  "private": true,
  "description": "Private Sadady Salla theme with static home and API-backed customer journeys",
  "scripts": {
    "validate": "node ./scripts/validate-theme.mjs",
    "lint": "npm run validate",
    "development": "node -e \"require('fs').mkdirSync('public', { recursive: true }); console.log('Sadady theme development bootstrap ready');\"",
    "watch": "node -e \"console.log('Sadady theme watch bootstrap ready');\"",
    "build": "npm run validate",
    "preview": "npm run validate"
  }
}
